const Projects = () => {
  // Charge un json qui contient les données des projets (screens, lien GH, lien live, petite description)

  return (
    <div className="projects">
      <h2>This is the Projects Component</h2>
    </div>
  );
};

export default Projects;
